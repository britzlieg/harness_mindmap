## 1. 打开思维导图文件流程

- [x] 1.1 新增或扩展 Electron 主进程打开文件处理器，弹出 `.mindmap` 选择器并通过 IPC 返回解析后的文件数据。
- [x] 1.2 在渲染进程接入“打开”命令（工具栏/菜单动作与命令分发），将返回的节点与元数据加载为当前活动文档。
- [x] 1.3 实现打开失败处理（取消/解析失败场景），确保应用保持可响应且当前内存导图状态不被破坏。

## 2. 隐藏英文原生菜单

- [x] 2.1 更新桌面启动菜单初始化逻辑，在支持平台上抑制英文原生顶层菜单（`File`、`Edit` 等）。
- [x] 2.2 验证菜单隐藏后核心命令（新建/打开/保存/导出/编辑）仍可通过现有应用内控件与快捷键访问。

## 3. 修复 PNG 导出渲染完整性

- [x] 3.1 重构 PNG 导出捕获路径，确保从包含节点文字与连线的完整渲染层截取。
- [x] 3.2 增加渲染就绪同步机制，使导出在捕获前等待最近编辑内容完成渲染。
- [x] 3.3 增加导出失败保护处理，避免崩溃并防止导出失败时污染当前编辑会话状态。

## 4. 测试覆盖与验证

- [x] 4.1 补充或更新文件操作测试，覆盖成功打开已有 `.mindmap` 与无效文件失败场景。
- [x] 4.2 补充或更新桌面菜单测试，断言运行时不显示英文原生顶层菜单标签。
- [x] 4.3 补充或更新 PNG 导出测试，验证生成图像包含文字与连线，且在编辑后立即导出仍正确。
