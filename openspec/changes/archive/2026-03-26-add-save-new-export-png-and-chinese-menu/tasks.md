## 1. 文件命令统一

- [x] 1.1 重构渲染层文件操作，使工具栏与 `Ctrl/Cmd+N` / `Ctrl/Cmd+S` 复用同一套新建/保存处理逻辑。
- [x] 1.2 更新 `MainToolbar`，提供明确的中文“新建/保存”动作，同时保持撤销/重做/新增子节点/导出/侧边栏等既有行为不变。
- [x] 1.3 确保在缺少活动文件上下文时保存命令安全失败（不崩溃、无未处理异常）。

## 2. PNG 导出实现

- [x] 2.1 在 Electron 导出服务中实现 PNG 生成，基于当前脑图导出数据并使用可复现的固定输出尺寸。
- [x] 2.2 接通 `export:saveAs` 的 PNG 分支，将 `.png` 写入用户选择路径，并与其他格式保持一致的成功返回语义。
- [x] 2.3 扩展渲染层导出弹窗，加入 PNG 可选项并使用中文操作文案。

## 3. 菜单中文化

- [x] 3.1 为命令/菜单类文案引入局部 UI 文案常量，避免字符串分散硬编码。
- [x] 3.2 将工具栏、导出弹窗、侧边栏布局/主题相关菜单标签替换为中文，同时保持内部值不变。
- [x] 3.3 更新相关 tooltip 与快捷键提示文案，实现命令名中文化但不改变实际快捷键。

## 4. 验证

- [x] 4.1 新增或更新测试，覆盖工具栏与快捷键两条路径下的新建/保存命令行为。
- [x] 4.2 新增或更新导出测试，覆盖 PNG 选项可见性与 Electron 处理器中的 PNG 文件生成路径。
- [x] 4.3 新增或更新 UI 文案断言，验证工具栏/导出/侧边栏菜单控件的中文标签。
- [x] 4.4 运行文件操作、导出处理与受影响 UI 组件的定向测试并修复回归。
