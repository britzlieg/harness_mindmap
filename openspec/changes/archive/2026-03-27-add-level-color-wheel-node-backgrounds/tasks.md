## 1. 颜色规则与共享能力

- [x] 1.1 在 `electron/shared/`（或共享主题工具模块）新增节点深度背景色计算函数，输入至少包含根节点基准色与节点深度。
- [x] 1.2 定义并落地固定色相步长常量，实现“按深度沿色轮相邻轮动（取模 360）”算法。
- [x] 1.3 保持显式 `backgroundColor` 覆盖优先级：仅对未显式设色节点应用自动轮动色。

## 2. 渲染链路接入

- [x] 2.1 将节点默认背景色来源切换为深度计算函数，移除或兼容旧的固定层级映射分支。
- [x] 2.2 校验节点重排/移动导致深度变化时，节点与子树默认背景色可触发重算并正确刷新。
- [x] 2.3 校验主题切换后根节点基准色变化能够传递到整棵树的层级轮动结果。

## 3. 测试与回归验证

- [x] 3.1 新增或更新单元测试：覆盖同层同色、相邻层级相邻色相、深度变化重算三类场景。
- [x] 3.2 新增或更新集成测试：覆盖显式背景色不被自动规则覆盖的场景。
- [x] 3.3 执行最小必要验证命令（相关测试 + `npm run typecheck`），并记录结果用于评审。
