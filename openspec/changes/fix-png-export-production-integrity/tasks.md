## 1. 边界与倍率语义修正

- [x] 1.1 重构 `fitSceneForRaster` 的边界扩容计算，按绝对栅格坐标覆盖右侧、底部以及负坐标偏移场景，消除“识别越界但未真正扩画布”的问题。
- [x] 1.2 提取并复用统一的 PNG 导出倍率归一化逻辑，让 `export-orchestrator` 与 `export-service` 共用同一套 `scale` 传递与安全收敛语义。
- [x] 1.3 检查 `scene-builder`、`png-renderer` 与兼容层的导出上下文传递，确保最终像素尺寸始终由“完整内容边界 × 归一化倍率”推导。

## 2. 分块捕获与诊断增强

- [x] 2.1 在 `png-renderer` 导出链路中引入结构化渲染诊断字段，至少覆盖命中路径、是否分块、fallback 原因与瓦片校验结果。
- [x] 2.2 将边缘瓦片尺寸不足的处理从静默 `resize` 改为受控校验流程，只允许裁剪过大瓦片，不允许拉伸过小瓦片。
- [x] 2.3 为瓦片尺寸异常实现受控重抓或降级分支，并将触发原因写入统一日志/诊断结果。

## 3. 测试补齐到真实生产链路

- [x] 3.1 为边界扩容新增定向测试，覆盖正坐标轻微越过右边界、下边界以及负坐标偏移的导出场景。
- [x] 3.2 新增 `export-orchestrator -> png-renderer` 的集成测试，显式断言本次导出命中的渲染路径或 fallback 原因。
- [x] 3.3 调整现有 `export-service` 与 IPC 相关测试，使其继续提供兼容回归保护，同时不再掩盖生产链路的倍率与渲染行为。

## 4. 验证与收尾

- [x] 4.1 运行最小必要测试集，至少覆盖 PNG 导出服务测试、真实生产链路测试与相关 IPC 测试。
- [x] 4.2 回看架构边界与共享类型落点，确认新增诊断字段与导出语义没有绕过 `electron/shared` 或破坏现有分层约束。
